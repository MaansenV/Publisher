---
title: Interfaces
description: Public interfaces outline for Easy FP Full Body Controller. Placeholder API pending verification against the asset source.
---

# Interfaces

::: warning API not yet verified
No class, method, or field name on this page has been checked against the shipped package. The tables below show the **shape** the interface documentation will take. Until each entry is verified against the actual C# source, treat them as illustrative, not authoritative.
:::

## Overview

The controller exposes its configuration and runtime hooks through a set of public components and (likely) scriptableObject settings. The exact types will be documented here once read from the package.

## Core component

`[to verify]` The main MonoBehaviour that drives the controller.

| Member | Type | Description |
| --- | --- | --- |
| `[to verify]` | `[to verify]` | `[to verify]` |

## Input

`[to verify]` How movement, look, and action input is consumed.

| Member | Type | Description |
| --- | --- | --- |
| `[to verify]` | `[to verify]` | `[to verify]` |

## Camera

`[to verify]` First-person camera setup and any exposed properties.

| Member | Type | Description |
| --- | --- | --- |
| `[to verify]` | `[to verify]` | `[to verify]` |

## Body / animation

`[to verify]` Full-body rendering and animation hooks.

| Member | Type | Description |
| --- | --- | --- |
| `[to verify]` | `[to verify]` | `[to verify]` |

## Events

`[to verify]` Any C# events, UnityEvents, or listener interfaces the asset fires.

| Event | Payload | When |
| --- | --- | --- |
| `[to verify]` | `[to verify]` | `[to verify]` |

---

::: details How this page gets filled in
Open the package source, list every `public` member users are meant to touch, and replace each `[to verify]` row with the real signature plus a one-line description. Remove the warning banner once every entry is verified.
:::
